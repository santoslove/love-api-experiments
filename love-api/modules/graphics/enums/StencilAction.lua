return {
    name = 'StencilAction',
    description = 'How a stencil function modifies the stencil values of pixels it touches.',
    descriptiont = {
        de = 'Wie eine Schablonenfunktion die Schablonenwerte von Pixeln modifiziert, die sie berührt. [auto]',
        fr = 'Comment une fonction de stencil modifie les valeurs de stencil des pixels qu\'il touche. [auto]',
        ja = 'オブジェクトに接触しているピクセルのステンシル値をステンシル関数で変更する方法。',
        ko = '스텐실 함수가 닿는 픽셀의 스텐실 값을 수정하는 방법 [auto]',
        pt = 'Como uma função de estêncil modifica os valores de estêncil de pixels que ele toca. [auto]',
        ru = 'Как функция трафарета изменяет значения трафаретов пикселей, к которым он прикасается. [auto]',
    },
    minidescription = 'How a stencil function modifies the stencil values of pixels it touches.',
    minidescriptiont = {
        de = 'Wie eine Schablonenfunktion die Schablonenwerte von Pixeln modifiziert, die sie berührt. [auto]',
        fr = 'Comment une fonction de stencil modifie les valeurs de stencil des pixels qu\'il touche. [auto]',
        ja = 'オブジェクトに接触しているピクセルのステンシル値をステンシル関数で変更する方法。',
        ko = '스텐실 함수가 닿는 픽셀의 스텐실 값을 수정하는 방법 [auto]',
        pt = 'Como uma função de estêncil modifica os valores de estêncil de pixels que ele toca. [auto]',
        ru = 'Как функция трафарета изменяет значения трафаретов пикселей, к которым он прикасается. [auto]',
    },
    constants = {
        {
            name = 'replace',
            description = 'The stencil value of a pixel will be replaced by the value specified in love.graphics.stencil, if any object touches the pixel.',
            descriptiont = {
                de = 'Der Schablonenwert eines Pixels wird durch den in love.graphics.stencil angegebenen Wert ersetzt, falls ein Objekt das Pixel berührt. [auto]',
                fr = 'La valeur du stencil d\'un pixel sera remplacée par la valeur spécifiée dans love.graphics.stencil, si un objet touche le pixel. [auto]',
                ja = 'ピクセルにオブジェクトが接触している場合、ピクセルのステンシル値は love.graphics.stencil により指定された値で置換されます。',
                ko = '픽셀의 스텐실 값은 객체가 픽셀에 닿으면 love.graphics.stencil에 지정된 값으로 대체됩니다. [auto]',
                pt = 'O valor do estêncil de um pixel será substituído pelo valor especificado em love.graphics.stencil, se algum objeto tocar o pixel. [auto]',
                ru = 'Значение трафарета для пикселя будет заменено значением, указанным в love.graphics.stencil, если какой-либо объект касается пикселя. [auto]',
            },
        },
        {
            name = 'increment',
            description = 'The stencil value of a pixel will be incremented by 1 for each object that touches the pixel. If the stencil value reaches 255 it will stay at 255.',
            descriptiont = {
                de = 'Der Schablonenwert eines Pixels wird für jedes Objekt, das das Pixel berührt, um 1 erhöht. Wenn der Schablonenwert 255 erreicht, bleibt er bei 255. [auto]',
                fr = 'La valeur du stencil d\'un pixel sera incrémentée de 1 pour chaque objet qui touche le pixel. Si la valeur du stencil atteint 255, elle restera à 255. [auto]',
                ja = 'ピクセルに接触している各オブジェクトに対してステンシル値は 1 加算されます。ステンシル値が 255 まで到達した場合は 255 のままになります。',
                ko = '픽셀의 스텐실 값은 픽셀에 닿는 각 객체에 대해 1 씩 증가합니다. 스텐실 값이 255에 도달하면 255로 유지됩니다. [auto]',
                pt = 'O valor do estêncil de um pixel será incrementado em 1 para cada objeto que toque o pixel. Se o valor do estêncil atingir 255 ele permanecerá em 255. [auto]',
                ru = 'Значение трафарета для пикселя будет увеличено на 1 для каждого объекта, который касается пикселя. Если значение трафарета достигает 255, оно останется на уровне 255. [auto]',
            },
        },
        {
            name = 'decrement',
            description = 'The stencil value of a pixel will be decremented by 1 for each object that touches the pixel. If the stencil value reaches 0 it will stay at 0.',
            descriptiont = {
                de = 'Der Schablonenwert eines Pixels wird für jedes Objekt, das das Pixel berührt, um 1 dekrementiert. Wenn der Schablonenwert 0 erreicht, bleibt er bei 0. [auto]',
                fr = 'La valeur du pochoir d\'un pixel sera décrémentée de 1 pour chaque objet qui touche le pixel. Si la valeur du stencil atteint 0, elle restera à 0. [auto]',
                ja = 'ピクセルに接触している各オブジェクトに対してステンシル値は 1 減算されます。 ステンシル値が 0 まで到達した場合は 0 のままになります。',
                ko = '픽셀의 스텐실 값은 픽셀에 닿는 각 객체에 대해 1 씩 감소합니다. 스텐실 값이 0에 도달하면 0으로 유지됩니다. [auto]',
                pt = 'O valor do estêncil de um pixel será diminuído em 1 para cada objeto que toque o pixel. Se o valor do estêncil atingir 0 ele permanecerá em 0. [auto]',
                ru = 'Значение трафарета пикселя будет уменьшаться на 1 для каждого объекта, который касается пикселя. Если значение трафарета достигнет 0, оно останется равным 0. [auto]',
            },
        },
        {
            name = 'incrementwrap',
            description = 'The stencil value of a pixel will be incremented by 1 for each object that touches the pixel. If a stencil value of 255 is incremented it will be set to 0.',
            descriptiont = {
                de = 'Der Schablonenwert eines Pixels wird für jedes Objekt, das das Pixel berührt, um 1 erhöht. Wenn ein Schablonenwert von 255 inkrementiert wird, wird er auf 0 gesetzt. [auto]',
                fr = 'La valeur du stencil d\'un pixel sera incrémentée de 1 pour chaque objet qui touche le pixel. Si une valeur de stencil de 255 est incrémentée, elle sera définie sur 0. [auto]',
                ja = 'ピクセルに接触している各オブジェクトに対してステンシル値は 1 加算されます。ステンシル値として 255 が加算される場合は 0 を設定します。',
                ko = '픽셀의 스텐실 값은 픽셀에 닿는 각 객체에 대해 1 씩 증가합니다. 255의 스텐실 값이 증가되면 0으로 설정됩니다. [auto]',
                pt = 'O valor do estêncil de um pixel será incrementado em 1 para cada objeto que toque o pixel. Se um valor de stencil de 255 for incrementado, ele será definido como 0. [auto]',
                ru = 'Значение трафарета для пикселя будет увеличено на 1 для каждого объекта, который касается пикселя. Если значение трафарета равно 255, оно будет установлено равным 0. [auto]',
            },
        },
        {
            name = 'decrementwrap',
            description = 'The stencil value of a pixel will be decremented by 1 for each object that touches the pixel. If the stencil value of 0 is decremented it will be set to 255.',
            descriptiont = {
                de = 'Der Schablonenwert eines Pixels wird für jedes Objekt, das das Pixel berührt, um 1 dekrementiert. Wenn der Schablonenwert von 0 dekrementiert wird, wird er auf 255 gesetzt. [auto]',
                fr = 'La valeur du pochoir d\'un pixel sera décrémentée de 1 pour chaque objet qui touche le pixel. Si la valeur du pochoir de 0 est décrémentée, elle sera définie sur 255. [auto]',
                ja = 'ピクセルに接触している各オブジェクトに対してステンシル値は 1 減算されます。ステンシル値として 0 が減算された場合は 255 を設定します。',
                ko = '픽셀의 스텐실 값은 픽셀에 닿는 각 객체에 대해 1 씩 감소합니다. 스텐실 값 0이 감소되면 255로 설정됩니다. [auto]',
                pt = 'O valor do estêncil de um pixel será diminuído em 1 para cada objeto que toque o pixel. Se o valor do estêncil de 0 for diminuído, ele será definido como 255. [auto]',
                ru = 'Значение трафарета пикселя будет уменьшаться на 1 для каждого объекта, который касается пикселя. Если значение трафарета равно 0, оно будет установлено на 255. [auto]',
            },
        },
        {
            name = 'invert',
            description = 'The stencil value of a pixel will be bitwise-inverted for each object that touches the pixel. If a stencil value of 0 is inverted it will become 255.',
            descriptiont = {
                de = 'Der Schablonenwert eines Pixels wird für jedes Objekt, das das Pixel berührt, bitweise invertiert. Wenn ein Schablonenwert von 0 invertiert wird, wird er 255. [auto]',
                fr = 'La valeur du pochoir d\'un pixel sera inversée par bit pour chaque objet qui touche le pixel. Si une valeur de pochoir de 0 est inversée, elle deviendra 255. [auto]',
                ja = 'ピクセルに接触している各オブジェクトに対してステンシル値はビットごとに反転されます。ステンシル値として 0 が反転された場合は 255 になります。',
                ko = '픽셀의 스텐실 값은 픽셀에 닿는 각 객체에 대해 비트 반전됩니다. 스텐실 값 0이 반전되면 255가됩니다. [auto]',
                pt = 'O valor do estêncil de um pixel será invertido no bit para cada objeto que toque o pixel. Se um valor de estêncil de 0 for invertido, ele se tornará 255. [auto]',
                ru = 'Значение трафарета пикселя будет побитовым инвертированным для каждого объекта, который касается пикселя. Если значение трафарета 0 инвертировано, оно станет 255. [auto]',
            },
        },
    },
}